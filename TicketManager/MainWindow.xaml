<Window x:Class="TicketManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TicketManager"
        mc:Ignorable="d"
        Title="Ticket Manager" Height="450" Width="800" Loaded="Window_Loaded" Closing="Window_Closing">
    <Grid>
        <Label Content="{Binding ProjectInfo.Name}" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top"/>
        <Menu Height="20" VerticalAlignment="Top">
            <MenuItem Header="Control">
                <MenuItem x:Name="NewProjectMenuItem" Header="プロジェクト作成" Click="NewProjectMenuItem_Click"></MenuItem>
                <MenuItem Header="プロジェクトを開く" ItemsSource="{Binding Projects}">
                    <MenuItem.ItemContainerStyle>
                        <Style TargetType="MenuItem">
                            <Setter Property="Header" Value="{Binding Name}" />
                            <Setter Property="Tag" Value="{Binding}" />
                            <EventSetter Event="Click" Handler="OpenProjectMenuItem_Click" />
                        </Style>
                    </MenuItem.ItemContainerStyle>
                </MenuItem>
                <MenuItem Header="チケットを作成" Click="NewTicketMenuItem_Click"/>
                <MenuItem Header="プロジェクト設定" Click="ProjectSettingsMenuItem_Click" />
            </MenuItem>
        </Menu>

    </Grid>
</Window>
